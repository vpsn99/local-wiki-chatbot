# local-wiki-chatbot
A lightweight chatbot that answers questions from your **local documents** (PDF, DOCX, TXT) and/or **Wikipedia**.   It uses HuggingFace sentence embeddings for semantic search, ChromaDB for vector storage, and Gradio for a simple web UI.
